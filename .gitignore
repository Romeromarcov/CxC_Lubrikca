# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/
ENV/

# Test / coverage
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.tox/

# Secrets / config — NUNCA versionar
.env
*.env
!.env.example
credentials*.json
service_account*.json
secrets/
*.pem
*.key

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Datos locales / fixtures generados en vivo
data/*.local.json

# Hallazgos del monitor local de logs de Railway (scripts/monitor_railway_logs.ps1)
railway_monitor_findings.log

# Entorno de pruebas del blindaje (credenciales efimeras del Odoo de QA)
.env.qa

# Artefactos generados de la auditoria de blindaje (se regeneran con los scripts)
docs/blindaje/*.json

# El reporte de cobertura en JSON, que `scripts/dinero_sin_cubrir.py`
# consume para elegir la proxima pieza de la Fase 2.4. Se regenera con
# `pytest --cov-report=json` y cambia en cada corrida: versionarlo llenaria
# el historial de diffs de un megabyte que nadie lee.
coverage.json
