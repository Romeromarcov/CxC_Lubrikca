# Runtime dependencies — Sistema de Cobros y Conciliación Lubrikca (CxC)
# Python 3.11+

# HTTP para scraper de tasas (Binance P2P / BCV). Endpoints públicos, sin credenciales.
requests==2.32.3

# Carga de configuración por variables de entorno (no secretos en el repo).
python-dotenv==1.0.1

# FastAPI & Uvicorn para el Dashboard Web
fastapi==0.111.0
uvicorn==0.30.1

# Persistencia PostgreSQL -- único backend de Repository (Google Sheets se
# retiró por completo en agosto 2026). SQLAlchemy Core (no ORM completo):
# tablas tipadas y queries componibles, sin sesiones/mapeo automático.
# Driver sincrono (psycopg3, no asyncpg): Repository es una interfaz
# sincrona hoy, consumida sin await por el motor/sync/reconciliador -- un
# driver async forzaria a reescribir esas piezas, fuera de alcance por ahora.
sqlalchemy==2.0.35
psycopg[binary]==3.2.3
alembic==1.13.3

# Export a Excel (Cobranza, Ventas) -- sin dependencia de pandas.
openpyxl==3.1.5
