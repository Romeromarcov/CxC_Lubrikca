# Runtime dependencies — Sistema de Cobros y Conciliación Lubrikca (CxC)
# Python 3.11+

# HTTP para scraper de tasas (Binance P2P / BCV). Endpoints públicos, sin credenciales.
requests==2.32.3

# Cliente Google Sheets (espejo de Odoo + tablas de trabajo).
gspread==6.1.2
google-auth==2.35.0
# Solo para el modo OAuth de desarrollo (GspreadGateway.from_oauth). En
# producción (cuenta de servicio) no se usa.
google-auth-oauthlib==1.2.1

# Carga de configuración por variables de entorno (no secretos en el repo).
python-dotenv==1.0.1

# FastAPI & Uvicorn para el Dashboard Web
fastapi==0.111.0
uvicorn==0.30.1
